# #######################################################################################
# AUTOGENERATED HYPRLAND CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hyprland.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=DP-1,3440x1440@174.96299,0x0,1,vrr,2
monitor=HDMI-A-2,preferred,-1920x300,auto
# monitor=HDMI-A-1,preferred,-5760x800,1
monitor=HDMI-A-1, disable
# monitor=HEADLESS-2,1920x1080@60,3440x0,auto
monitor=,preferred,auto-left,1

###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = uwsm app -- alacritty
$fileManager = uwsm app -- nemo
# $menu = wofi --show drun


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# screenshare?
exec-once=dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

exec-once = kwalletd6
exec-once = hyprctl setcursor Bibata-Modern-Ice 24
exec-once = uwsm app -- ags run --gtk4
exec-once = uwsm app -- dunst
exec-once = uwsm app -- waypaper --restore
exec-once = [workspace 1 silent] uwsm app -- zen-browser
exec-once = [workspace special:spotify silent] uwsm app -- spotify-launcher
exec-once = [workspace special:discord silent] uwsm app -- goofcord


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general

source = ./styles/macos.style.conf

general {
    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = true
    windowrulev2 = immediate, class:steam_app_.*

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = true

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 0, 3.81, easeOutQuint
    animation = layersIn, 0, 4, easeOutQuint, fade
    animation = layersOut, 0, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 2, easeInOutCubic, slidevert
    animation = workspacesIn, 1, 1.21, almostLinear, slidevert
    animation = workspacesOut, 1, 1.94, almostLinear, slidevert
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrulev2 = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrulev2 = rounding 0, floating:0, onworkspace:w[tv1]
# windowrulev2 = bordersize 0, floating:0, onworkspace:f[1]
# windowrulev2 = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
    middle_click_paste = false
}

binds {
  workspace_back_and_forth = true
}

cursor {
  inactive_timeout = 1
  hide_on_key_press = true
}

# plugin {
#     hyprwinwrap {
#         title = hyprwallpapr
#     }
# }
# exec-once = nice mpv --title=hyprwallpapr --no-audio --loop=inf /home/icarus/Wallpapers/4B9FSlM0SgT0rkY_Drawings\ Under\ the\ rain\ Live\ Wallpaper.mp4

# hdr
# env = DXVK_HDR,1
# env = ENABLE_HDR_WSI,1
# experimental {
#   hdr = true
#   wide_color_gamut = true
#   xx_color_management_v4 = true
# }


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options = grp:altgr, compose:menu
    kb_rules =

    follow_mouse = 1

    sensitivity = -0.5 # -1.0 - 1.0, 0 means no modification.

    # scroll_method = on_button_down
    # scroll_button = 1

    touchpad {
        natural_scroll = false
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
# gestures {
#     workspace_swipe = false
# }

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
# device {
#     name = epic-mouse-v1
#     sensitivity = -0.5
# }


###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier
$primaryMonitor = DP-1
$secondaryMonitor = HDMI-A-2
$penDisplay = HDMI-A-1

# disable middle mouse paste
bindn = , mouse:274, exec, wl-copy -pc

# swap to kde session
bind = $mainMod, slash, exec, sudo /usr/bin/chvt 3

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Q, exec, $terminal
bind = $mainMod, N, exec, $fileManager
bind = $mainMod, W, exec, uwsm app -- waypaper
bind = $mainMod, SPACE, exec, tofi-drun | xargs hyprctl dispatch exec uwsm app --
bind = $mainMod, I, exec, hyprpicker | wl-copy
bind = $mainMod SHIFT, I, exec, ~/.config/hypr/scripts/replace-active-color.sh

bind = $mainMod, backslash, killactive,
bind = $mainMod SHIFT, C, forcekillactive,

bind = $mainMod, V, togglefloating,
bind = $mainMod, P, pin, # dwindle

bind = $mainMod, F, fullscreen, 1
bind = $mainMod SHIFT, F, fullscreen, 0

# bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, U, togglesplit, # Dwindle-Layout

# Move focus with mainMod + arrow keys
bind = $mainMod, H, movefocus, l
bind = $mainMod, J, movefocus, d
bind = $mainMod, K, movefocus, u
bind = $mainMod, L, movefocus, r

bind = $mainMod SHIFT, H, swapwindow, l
bind = $mainMod SHIFT, J, swapwindow, d
bind = $mainMod SHIFT, K, swapwindow, u
bind = $mainMod SHIFT, L, swapwindow, r

# Switch monitors
bind = $mainMod ALT, comma, focusmonitor, +1
bind = $mainMod ALT, period, focusmonitor, -1
bind = $mainMod, comma, focusmonitor, $secondaryMonitor
bind = $mainMod, period, focusmonitor, $primaryMonitor

bind = $mainMod ALT SHIFT, comma, movewindow, mon:+1
bind = $mainMod ALT SHIFT, period, movewindow, mon:-1
bind = $mainMod SHIFT, comma, movewindow, mon:$secondaryMonitor
bind = $mainMod SHIFT, period, movewindow, mon:$primaryMonitor

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

bind = $mainMod, S, togglespecialworkspace, spotify
bind = $mainMod SHIFT, S, movetoworkspace, special:spotify

bind = $mainMod, D, togglespecialworkspace, discord
bind = $mainMod SHIFT, D, movetoworkspace, special:discord

bind = $mainMod, A, togglespecialworkspace, magic
bind = $mainMod SHIFT, A, movetoworkspace, special:magic

workspace = name:gaming, monitor:$primaryMonitor
bind = $mainMod, G, workspace, name:gaming
bind = $mainMod SHIFT, G, movetoworkspace, name:gaming

# workspace = name:sunshine, monitor:$secondaryMonitor
# bind = $mainMod ALT, G, workspace, name:sunshine
# bind = $mainMod ALT SHIFT, G, movetoworkspace, name:sunshine

workspace = 1, monitor:$primaryMonitor
workspace = 2, monitor:$primaryMonitor
workspace = 3, monitor:$primaryMonitor
workspace = 4, monitor:$primaryMonitor
workspace = 5, monitor:$primaryMonitor
workspace = 6, monitor:$secondaryMonitor
workspace = 7, monitor:$secondaryMonitor

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, m+1
bind = $mainMod, mouse_up, workspace, m-1

bind = $mainMod, =, workspace, m+1
bind = $mainMod, -, workspace, m-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
# bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
# bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 10%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl -p spotify next
bindl = , XF86AudioPause, exec, playerctl -p spotify play-pause
bindl = , XF86AudioPlay, exec, playerctl -p spotify play-pause
bindl = , XF86AudioPrev, exec, playerctl -p spotify previous
bindel = ,XF86AudioRaiseVolume, exec, playerctl -p spotify volume 0.02+
bindel = ,XF86AudioLowerVolume, exec, playerctl -p spotify volume 0.02-

# Passthrough to discord
bind = $mainMod, M, sendshortcut, CONTROL SHIFT, M, class:goofcord

# Screenshot
bind = $mainMod, Z, exec, grim -g "$(hyprctl activewindow -j | jq -r '"\(.at[0]),\(.at[1]) \(.size[0])x\(.size[1])"')" - | wl-copy
bind = $mainMod SHIFT, Z, exec, grim -g "$(slurp)" - | wl-copy
bind = $mainMod SHIFT CONTROL, Z, exec, grim -o "$(hyprctl monitors -j | jq -r '.[] | select(.focused) | .name')" - | wl-copy

# Search scratchpad submap
bind = SUPER, SUPER_L, exec, sleep 0.2 && hyprctl dispatch submap reset
bindr = SUPER, SUPER_L, submap, search
submap = search
bind = , SUPER_L, exec, ~/.config/hypr/scripts/search-scratchpad.sh
submap = reset

# Resize submap
bindr = $mainMod, R, submap, resize
submap = resize
binde = , H, resizeactive, -20 0
binde = , L, resizeactive, 20 0
binde = , K, resizeactive, 0 -20
binde = , J, resizeactive, 0 20
binde = SHIFT, H, resizeactive, -200 0
binde = SHIFT, L, resizeactive, 200 0
binde = SHIFT, K, resizeactive, 0 -200
binde = SHIFT, J, resizeactive, 0 200
bindr = $mainMod, R, submap, reset
submap = reset

### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$

# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
windowrulev2 = workspace special:discord silent, class:goofcord
windowrulev2 = workspace special:spotify silent, class:spotify
windowrulev2 = float, class:waypaper

windowrulev2 = float, class:org.godotengine.ProjectManager
windowrulev2 = size 966 621, class:org.godotengine.ProjectManager

windowrulev2 = float, initialTitle:Calculator
windowrulev2 = size 660 790, initialTitle:Calculator

windowrulev2 = float, initialTitle:Secure CRAN mirrors
windowrulev2 = float, initialClass:PureRef

# yad
windowrulev2 = float, initialClass:yad
windowrulev2 = size 0 0, initialClass:yad
windowrulev2 = workspace special:search, initialTitle:search-scratchpad-input

windowrulev2 = float, initialClass:nemo
windowrulev2 = size 1089 652, initialClass:nemo

windowrulev2 = workspace name:gaming, class:steam_app_\d*
windowrulev2 = workspace name:gaming, class:gamescope
windowrulev2 = fullscreen, class:steam_app_\d*
windowrulev2 = norounding, class:steam, floating:1
windowrulev2 = noblur, class:steam, floating:1
windowrulev2 = workspace 5, class:steam, floating:0

windowrulev2 = float, initialTitle:negative:Steam, initialClass:steam
windowrulev2 = size 328 900, initialTitle:Friends List
windowrulev2 = move 3056 54, initialTitle:Friends List

# Ignore maximize requests from apps. You'll probably like this.
windowrulev2 = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrulev2 = nofocus, class:^$, title:^$, xwayland:1, floating:1, fullscreen:0, pinned:0

# fix spotify breaking my ears
exec-once = sleep 10 && playerctl -p spotify volume 0.25
